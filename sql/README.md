# SQLite 数据库说明

这个目录存放项目的 SQLite 数据库相关文件。

- `schema.sql`：建表语句，定义岗位、简历、匹配结果三张表。
- `queries.sql`：常用查询示例，方便在 PyCharm 的数据库控制台里复习和演示。
- `job_fit.db`：运行项目后自动生成的本地数据库文件，默认不会提交到 Git。

默认数据表：

- `jobs`：保存岗位 JD 来源和文本。
- `resumes`：保存简历文件名和简历文本。
- `matches`：保存简历与岗位的匹配分、等级、优势、短板、建议和原始结构化结果。
